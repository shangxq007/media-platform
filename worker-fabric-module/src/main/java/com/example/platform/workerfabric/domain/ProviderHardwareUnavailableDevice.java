package com.example.platform.workerfabric.domain;

/** Evidence that the exact assigned device exists but is currently unavailable. */
public record ProviderHardwareUnavailableDevice() implements ProviderHardwareDeviceEvidence {}
