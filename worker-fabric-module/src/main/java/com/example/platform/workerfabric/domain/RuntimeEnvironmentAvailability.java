package com.example.platform.workerfabric.domain;

/** Current availability of the provider runtime mechanics on a worker endpoint. */
public enum RuntimeEnvironmentAvailability {
    AVAILABLE,
    UNAVAILABLE,
    UNKNOWN
}
