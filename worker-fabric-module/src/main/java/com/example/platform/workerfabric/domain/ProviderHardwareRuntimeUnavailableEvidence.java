package com.example.platform.workerfabric.domain;

/** Known evidence that the exact worker runtime is unavailable. */
public record ProviderHardwareRuntimeUnavailableEvidence() implements ProviderHardwareProbeEvidence {}
