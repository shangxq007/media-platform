package com.example.platform.workerfabric.domain;

/** Evidence that the exact assigned device is not exposed to the worker runtime. */
public record ProviderHardwareNotExposedDevice() implements ProviderHardwareDeviceEvidence {}
