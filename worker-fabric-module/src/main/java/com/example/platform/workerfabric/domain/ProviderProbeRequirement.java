package com.example.platform.workerfabric.domain;

/** Whether backend runtime policy requires fresh provider probe evidence for this evaluation. */
public enum ProviderProbeRequirement {
    REQUIRED,
    NOT_REQUIRED
}
