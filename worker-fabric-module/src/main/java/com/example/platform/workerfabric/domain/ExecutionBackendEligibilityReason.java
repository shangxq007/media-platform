package com.example.platform.workerfabric.domain;

/** Typed execution-mechanics legality reasons; never capability semantics. */
public enum ExecutionBackendEligibilityReason {
    BACKEND_EXECUTION_MECHANICS_UNSUPPORTED,
    UNKNOWN_BACKEND_EXECUTION_SUPPORT
}
