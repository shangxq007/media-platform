package com.example.platform.workerfabric.domain;

/** Server-side validation disposition for non-authoritative runtime support evidence. */
public enum RuntimeSupportAdvertisementReason {
    ACCEPTED_CANDIDATE_EVIDENCE,
    REQUIREMENT_MISSING,
    MISSING,
    RUNTIME_MISMATCH,
    UNSUPPORTED
}
