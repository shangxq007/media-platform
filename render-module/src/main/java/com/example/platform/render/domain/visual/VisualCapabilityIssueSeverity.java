package com.example.platform.render.domain.visual;

/**
 * Severity for visual capability issues.
 * Immutable enum. Internal domain model.
 */
public enum VisualCapabilityIssueSeverity {
    INFO,
    WARNING,
    ERROR,
    BLOCKING
}
