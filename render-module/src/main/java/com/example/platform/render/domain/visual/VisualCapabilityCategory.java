package com.example.platform.render.domain.visual;

/**
 * Visual capability category taxonomy.
 * Immutable enum. Internal domain model.
 */
public enum VisualCapabilityCategory {
    CAPTION,
    WATERMARK,
    OVERLAY,
    EFFECT,
    TRANSITION,
    COLOR,
    TRANSFORM,
    AUDIO,
    PACKAGING
}
