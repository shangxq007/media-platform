package com.example.platform.render.domain.asset.marketplace;

/**
 * Marketplace listing types — maps to asset types.
 */
public enum MarketplaceListingType {
    MEDIA,
    TIMELINE_TEMPLATE,
    EFFECT,
    PLUGIN,
    STYLE,
    WORKFLOW,
    AI_MODEL
}
