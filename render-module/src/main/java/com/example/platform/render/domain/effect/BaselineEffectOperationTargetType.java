package com.example.platform.render.domain.effect;

/**
 * Target types for baseline effect operations.
 * Immutable enum. Internal domain model.
 */
public enum BaselineEffectOperationTargetType {
    TIMELINE,
    TRACK,
    CLIP,
    CAPTION,
    WATERMARK,
    OVERLAY
}
