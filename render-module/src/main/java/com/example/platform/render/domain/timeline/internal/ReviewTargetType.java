package com.example.platform.render.domain.timeline.internal;

/**
 * Target types for review workflow — enables a single review system for all content.
 */
public enum ReviewTargetType {
    TIMELINE,
    ASSET,
    TEMPLATE,
    EFFECT,
    PLUGIN,
    WORKFLOW,
    AI_MODEL
}
