package com.example.platform.render.domain.transition;

/**
 * Target types for baseline transition operations.
 * Immutable enum. Internal domain model.
 */
public enum BaselineTransitionOperationTargetType {
    TIMELINE,
    TRACK,
    CLIP_PAIR,
    TRANSITION
}
