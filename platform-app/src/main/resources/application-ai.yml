# Profile: direct OpenAI / OpenAI-compatible endpoint (NIM, etc.)
# Usage: --spring.profiles.active=dev,ai

app:
  ai:
    default-provider: unavailableChatProvider
    providers:
      openai:
        enabled: true
        backend-label: openai
    routing:
      script-generation:
        primary:
          provider: openAiChatProvider
          model: gpt-4o-mini
        fallback:
          - provider: unavailableChatProvider
      nlq-sql-generation:
        primary:
          provider: openAiChatProvider
          model: gpt-4o-mini
      nlq-result-summary:
        primary:
          provider: openAiChatProvider
          model: gpt-4o-mini

spring:
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:}
      base-url: ${OPENAI_BASE_URL:https://api.openai.com/v1}
