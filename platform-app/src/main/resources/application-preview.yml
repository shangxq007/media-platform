spring:
  task:
    scheduling:
      enabled: false
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:dev-stub-key}
      chat:
        options:
          model: gpt-4o-mini

platform:
  preview:
    enabled: true

  adaptive-engine:
    enabled: false

  ai:
    enabled: false

  workflow:
    enabled: false

  scheduler:
    enabled: false

  worker:
    enabled: false

  worker-queue:
    enabled: false

delivery:
  worker-enabled: false

  payment:
    enabled: false

  commerce:
    enabled: false

  social-publish:
    enabled: false

  sandbox:
    enabled: false

  cloud-resource:
    enabled: false

  provider:
    fallback-enabled: false
    auto-discovery-enabled: false

  render:
    adaptive-routing-enabled: false
    provider-fallback-enabled: false
    mode: local

  billing:
    enforcement-enabled: false

  entitlement:
    enforcement-enabled: false

app:
  security:
    enabled: false
    dev-auth-endpoint: false
    oidc-dev-bootstrap:
      enabled: false
  identity:
    api-key-auth-enabled: false
  secrets:
    vault:
      enabled: false
  sandbox:
    enabled: false
