/**
 * Derived application projection over independent capability, H1, Entitlement,
 * Quota, and role/workspace policy decisions. This package owns no source authority
 * and exposes a pure, read-only view.
 */
package com.example.platform.capability.effective;
