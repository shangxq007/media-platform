package com.example.platform.storage.delivery.contract;

public enum AccessDescriptorContractType {
    SIGNED_URL, INTERNAL_STREAM, EXTERNAL_BUCKET, EXPORT_BUNDLE, NO_PUBLIC_ACCESS, NOT_USER_FACING
}
