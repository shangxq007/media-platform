package com.example.platform.bmf;

/** BMF-local fingerprint evidence diagnostics; not runtime compatibility authority. */
public enum BmfCpuRuntimeEvidenceIssue {
    MISSING_FINGERPRINT,
    FINGERPRINT_MISMATCH
}
