apiVersion: v1
kind: Service
metadata:
  name: render-worker
  namespace: media-platform
spec:
  selector:
    app: media-platform
    component: render-worker
  ports:
    - port: 80
      targetPort: 8081
  type: ClusterIP
