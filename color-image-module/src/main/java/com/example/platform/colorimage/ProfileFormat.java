package com.example.platform.colorimage;

/**
 * ROADMAP_18 (CIC3): explicit profile semantic family. REQUIRED on every
 * ProfileBasedColorDescription. V1 = ICC. Identifies semantic family, NOT an
 * execution provider (never LittleCMS/FFmpeg/GPU selection).
 */
public enum ProfileFormat {
    ICC
}
