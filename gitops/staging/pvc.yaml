apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: app-data
  namespace: media-platform-staging
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 50Gi
  storageClassName: nfs-client
