apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: media-platform-staging
spec:
  selector:
    app: media-platform
    component: api
  ports:
    - port: 80
      targetPort: 8080
  type: ClusterIP
