# Domain authority targeted tests

Use `bash scripts/test-authority-modules.sh billing` for EP32 / DEC-COMP-15/16. It verifies unique platform usage repository/publisher/port beans, AI/Render/PluginRuntime wiring, real migrated PostgreSQL observation/outbox idempotency and tenant isolation, missing-database startup failure, existing usage persistence/rollback tests and actual runtime consumer tests. The extension composition must supply the platform emission port to RuntimeUsageEmitter. The canonical V1 outbox schema must include the error columns used when replaying a pending event; test-only DDL is insufficient. This repository's canonical V1 and generated jOOQ schema remain aligned; no live database is altered by this test command. Platform scanning is unchanged, no provider is invoked, and no new billing decision or metering authority is introduced.

Use `bash scripts/test-authority-modules.sh observation` for EP17: Observability context/filter lifecycle and thread isolation, Audit adapters, real PostgreSQL API-key authentication and rejection, JWT/OIDC/Delivery/admin composition, and Modulith. `ObservationBoundaryTest` checks the published context boundary and proves the architecture rule rejects an internal dependency. The API-key actor comes from the authenticated request, never diagnostic MDC; no worker-thread identity propagation is implied. The snapshot is immutable diagnostic data, not permission or business authority. Existing trace-key strings and request cleanup remain unchanged. No new async executor or context propagation mechanism is introduced.

Use `bash scripts/test-authority-modules.sh identity` for Identity-owned authorization contracts and their current Delivery/Workflow/Render/Platform consumers. Each Gradle `--tests` selection applies only to the preceding project; Delivery and Identity run their whole module tests. The command exercises RBAC allow/deny, tenant/project boundaries, authenticated management/admin composition, Workflow vocabulary, Entitlement collaboration and independent commercial decisions, denial transport, and Modulith.

Run `bash scripts/test-authority-modules.sh compile` for changed published-contract consumers and current backend CI compilation/PFIRR1/bootJar gates. This is not a full test suite. Current `.github/workflows/ci.yml` backend also requires Docker image smoke build; run once on the accepted combined backend candidate, with a task-local image name, no publish/deploy. Preserve existing resource/fork policy; do not enable broad JUnit parallelism.

Isolation: execute in the task-owned worktree. Tests requiring PostgreSQL use existing Testcontainers with private databases/containers and dynamically assigned ports. Do not use `scripts/local-test.sh` against shared Docker Compose services: that script stops/removes its configured database. No real provider, production account, application dataset or another session's ports may be used. Worktree Gradle outputs are task-local; a dependency artifact cache may be reused but never another writer's build outputs.

For a small correction rerun the changed module/tests and real affected consumers. A new commit or FF does not by itself change behavior: distinguish actual execution, Gradle UP-TO-DATE with identical inputs, and previously accepted results. Source scans/compilation do not replace runtime allow/deny tests. Add filters when a new actual caller is introduced; remove obsolete filters only with an identified replacement and preserved coverage. Expand beyond these groups for a concrete uncovered risk or current required CI gate, recording the reason before execution. Historical per-epoch Hermes/FCV/full-suite scheduling text is not the current Owner's lightweight execution policy.

EP27A dispositions: Identity publishes AuthorizationDecisionPort, CanonicalActorResolver and a transport-independent typed denial. Workflow owns AuthorizationActions. Entitlement owns its consumer-specific CollaborationAccessPort; the existing external Identity adapter remains composition, not another authorization implementation. Stable ActorType/CanonicalActor, action/resource/request/context and decision values have immutable bounded data semantics and cross-domain producers/consumers, so they remain shared for this slice. Other shared concerns are not thereby approved as permanent kernel responsibilities.

DEC-SHARED-001: the global arbitrary prefix-string `Ids` generator is deleted. Existing typed domain/application construction paths now generate their own fixed-namespace UUID value directly, preserving the exact namespace and entropy/format; no replacement global generator, typed namespace registry, compatibility alias or new domain model is introduced. Existing foreign domain writers are not legitimized by this mechanical change and remain assigned to their original EPs. Test-only fixture labels remain local test data. Domain-local creation and persistence tests are relevant to this change; identical UUID construction is not a database migration.

Frontend files and dashboard contracts are not touched by this backend slice. Reuse accepted A/B 200 tests, overlapping targeted groups and isolated JWT/PostgreSQL dashboard results unless a changed actual authentication/composition path invalidates the relevant subset. These results do not establish production OIDC, silent revocation, Project Open, canonical Canvas or provider publication.

EP05: `bash scripts/test-authority-modules.sh execution` verifies the sole platform task-backend composition, actual Whisper lookup, canonical local sandbox result propagation, unsupported BMF rejection/cancellation/identity checks, and current BMF/provider runtime tests. Outbox no longer contains concrete execution backends or their registry implementation. The neutral existing request/result/capability/registry port is exported by sandbox::Execution; implementation/registration is runtime-owned. BMF remains unsupported as declared by its accepted CPU seam; the removed Outbox sleep-and-success stub was not real execution. No attempt/generation/completion owner is changed. Provider runtime alone may depend on the BMF binding; core/H1 no-private-topology assertions remain. Added explicit Modulith interfaces expose already cross-module runtime values rather than expanding a violation allowlist.

Changes to these ports, runtime composition, BMF binding, sandbox result semantics or Whisper callers invalidate EP05 evidence; include the affected runtime consumer and Modularity checks. PostgreSQL tests use execution-owned Testcontainers; no shared service or real provider call. Neutral SPI relocation must not create an Outbox→provider runtime dependency or a duplicate registry. Existing local ASR calls without an explicit workspace remain fail-closed; this slice does not add Whisper workspace management.
