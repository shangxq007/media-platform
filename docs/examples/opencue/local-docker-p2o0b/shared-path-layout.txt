P2O.0b — Shared Path Layout

Host shared root:
  build/opencue-shared/media-platform-smoke

Container shared root:
  /mnt/opencue-shared/media-platform-smoke

Directory layout (identical on host and container):

  media-platform-smoke/
    jobs/
      smoke-001/
        input/
          input-fixture.mp4          # generated by smoke level 2
        work/
          caption-overlay-input.ass  # generated by smoke level 2
        output/
          output.mp4                 # ffmpeg render output
          local-render-execution-report.txt  # smoke report
        logs/
          shared-path-probe.txt      # smoke level 0
          ffprobe-output.txt         # smoke level 1
          ffprobe-input.txt          # smoke level 2
          ffprobe-output.txt         # smoke level 2
          ffmpeg.stdout.log          # ffmpeg stdout
          ffmpeg.stderr.log          # ffmpeg stderr
          opencue-job.log            # OpenCue job log
          cuebot.log                 # Cuebot log (if collected)
          rqd.log                    # RQD log (if collected)

Runtime submission:
  Scripts run inside RQD container or submitted via Cuebot.
  Shared path ensures host can inspect all outputs.

Transition to PVE:
  Local Docker host root: build/opencue-shared/media-platform-smoke
  Future PVE root: /mnt/opencue-shared/media-platform-smoke
  Same logical layout, different physical mount.
