# Release Candidate: P4 Import/Export Pipeline

**Date:** 2026-06-06
**Tag:** rc/p4-import-export-2026-06-06.3 (latest), rc/p4-import-export-2026-06-06.2, rc/p4-import-export-2026-06-06
**Commit:** 001bb26
**Status:** RC only - not production-ready

## Scope

This RC covers the P4 Import/Export pipeline for the audio/video editing platform:

1. **Project Export** - JSON and ZIP export with metadata_only and linked_assets modes
2. **Project Import Preview** - JSON and ZIP preview without side effects
3. **Project Import Execute Shell** - ZIP shell import creating project shell with metadata persistence
4. **Import Metadata Read API** - Summary and detail endpoints for persisted import metadata
5. **Frontend Metadata Preview** - UI panel showing imported metadata status
6. **Security Hardening** - Tenant isolation, URL scrubbing, audit safety, zip security

## Completed Capabilities

### Export
- JSON export (metadata_only, linked_assets)
- ZIP export archive endpoint
- Signed URL generation for linked_assets (3600s default TTL, 86400s max)
- Checksum validation (sha256sums.txt)
- Zip slip/bomb protection
- Filename sanitization

### Import Preview
- JSON import preview
- ZIP import preview with ProjectExportZipReader
- Entry allowlist validation
- Checksum verification
- No DB writes, no URL downloads

### Import Execute Shell
- ZIP shell import creating project shell
- All assets marked needs_upload
- Metadata persistence (timeline, render, spatial, effects, asset mapping)
- Transactional rollback on failure
- Audit with redaction guard

### Metadata Read API
- Summary endpoints (boolean flags only)
- Detail endpoints (scrubbed JSON content)
- Tenant-scoped queries
- Defensive scrub-on-read

### Frontend
- ImportedMetadataPanel integrated into ExportPanel
- Defense-in-depth frontend sanitization
- No editor timeline mutation
- No media download/upload

## Security Guarantees

- **Tenant isolation:** Path tenantId is sole trusted source; source tenantId from zip is ignored
- **URL safety:** No signed URLs in audit; storageUri/storageRef never exposed
- **Zip security:** Zip slip/bomb protection, checksum validation, entry allowlist
- **Scrub-on-write + scrub-on-read:** Metadata scrubbed before DB save and on read
- **Audit safety:** Payloads contain only safe fields (no URLs, no JSON content, no zip bytes)

## API Endpoints

| Endpoint | Method | Status |
|----------|--------|--------|
| `/tenants/{tenantId}/projects/{projectId}/exports` | POST | ✅ Stable |
| `/tenants/{tenantId}/projects/{projectId}/exports/archive` | POST | ✅ Stable |
| `/tenants/{tenantId}/project-imports/preview` | POST | ✅ Stable |
| `/tenants/{tenantId}/project-imports/preview/archive` | POST | ✅ Stable |
| `/tenants/{tenantId}/project-imports/archive` | POST | ✅ Stable |
| `/tenants/{tenantId}/projects/{projectId}/import-metadata` | GET | ✅ Stable |
| `/tenants/{tenantId}/project-imports/{importId}/metadata` | GET | ✅ Stable |
| `/tenants/{tenantId}/projects/{projectId}/import-metadata/detail` | GET | ✅ Stable |
| `/tenants/{tenantId}/project-imports/{importId}/metadata/detail` | GET | ✅ Stable |

## Test Results

| Check | Result | Notes |
|-------|--------|-------|
| Backend compile | ✅ SUCCESS | |
| Frontend compile | ✅ SUCCESS | |
| identity-access-module:test | ✅ 361 tests | All passing |
| Frontend typecheck | ✅ SUCCESS | 0 errors |
| ImportedMetadataPanel tests | ✅ 9/9 | Targeted logic tests |
| Golden Render E2E | ✅ 4/4 | Automated validation |

## Known Limitations

### Frontend
- Full vitest suite has pre-existing jsdom/happy-dom environment issues (not caused by P4 changes)
- ExportPanel integration tests deferred (requires Vue component mounting)

### Staging/Production
- Real domain not configured
- OIDC not configured
- Storage signing not configured
- Secrets not configured

### Feature Gaps (post-RC)
- Media asset import not implemented
- Bundled_assets mode not implemented
- Async export not implemented
- Editor/runtime metadata wiring not implemented
- Full project restoration not implemented

## Required Human Reviews

Before staging deployment:
1. **Security review sign-off** - Human review of security boundaries
2. **Golden Render visual QA** - Human inspection of rendered output
3. **Frontend test env review** - Fix pre-existing vitest environment issues
4. **DevOps staging inputs** - Configure OIDC, storage, secrets, domain

## Staging Blockers

- Infrastructure team must provide:
  - OIDC provider configuration
  - MinIO/S3 bucket configuration
  - Domain configuration
  - Secret management

## Production Blockers

- All staging blockers
- Production-specific secrets, monitoring, alerting
- Human security review sign-off
- Golden Render visual QA sign-off

## Post-RC Tasks

1. **P4-STAGING-INPUTS** - Configure staging environment
2. **P4-GOLDEN-HUMAN-QA** - Human visual inspection of Golden Render output
3. **P4-FE-FULL-VITEST-FIX** - Fix frontend test environment
4. **P4-IMPORT-RUNTIME-WIRING** - Wire imported metadata to editor/runtime
5. **P4-BUNDLED-ASSETS** - Implement bundled_assets import mode
6. **P4-ASYNC-EXPORT** - Implement async export for large projects
7. **P4-MEDIA-IMPORT** - Implement full media asset import

## Security Policy Details

See `docs/media-rendering/project-export.md` section "Security Policy Details" for:
- MetadataScrubber policy (including `key` field handling)
- linked_assets signed URL sharing semantics
- Metadata read audit policy

---

**This RC is a code freeze for review purposes only.**
**Not production-ready. Staging deployment requires infrastructure inputs.**

## RC2 Changes (rc/p4-import-export-2026-06-06.2)

**Commit:** `c0fa98d` - chore: prelaunch migration squash and V6 FK fix

### Fixes Included

1. **V6 Foreign Key Fix**
   - Changed composite FK `(project_id, tenant_id) → project(id, tenant_id)` to single FK `(project_id) → project(id)`
   - Fixes H2/PostgreSQL compatibility: `project` table PK is only on `id`
   - Error: `ERROR: there is no unique constraint matching given keys for referenced table "project"`

2. **Migration Archive**
   - V2 (`V2__backfill_audit_record_categories.sql`) → `docs/archive/prelaunch-migrations/`
   - V3 (`V3__enforce_audit_record_category_constraints.sql`) → `docs/archive/prelaunch-migrations/`
   - V4 (`V4__add_effect_taxonomy_fields.sql`) → `docs/archive/prelaunch-migrations/`
   - V5 (`V5__add_artifact_size_bytes_and_checksum.sql`) → `docs/archive/prelaunch-migrations/`
   - V1 (baseline) and V6 (P4 addition) retained in `db/migration/`

3. **Documentation**
   - `docs/releases/prelaunch-migration-squash-2026-06-06.md` - Migration squash documentation
   - `docs/releases/staging-readiness-gate-2026-06-06.md` - Staging readiness gate
   - `docs/releases/rc-human-signoff-2026-06-06.md` - Human sign-off preparation

### Known Remaining Issues

- 22 pre-existing `platform-app:test` failures (not caused by P4, confirmed on original code)
- Human security sign-off pending
- Golden Render visual QA pending
- Staging infrastructure inputs pending

### CI Status

| Pipeline | Status | Notes |
|----------|--------|-------|
| `./gradlew :identity-access-module:test` | ✅ 361/361 PASS | P4 core tests |
| `npm run typecheck` | ✅ 0 errors | Frontend |
| `./gradlew :platform-app:test` | ✅ BUILD SUCCESSFUL | |
| `./gradlew test` (full) | ⚠️ 22 pre-existing failures | 历史失败，与 P4 无关 |

### Pre-existing CI Failures

See `docs/releases/ci-preexisting-failures-2026-06-06.md` for:
- Full failure inventory (22 tests across 8 suites)
- Root cause analysis
- Evidence these predate P4 changes
- CI layering recommendations
- Risk acceptance options

---

## Current Status (2026-06-08)

| 维度 | 状态 | 说明 |
|------|------|------|
| **RC** | ✅ Ready | rc/p4-import-export-2026-06-06.3 |
| **Staging** | ⏳ Pending | 等待 infra inputs + human sign-off + human review |
| **Production** | ❌ Not Ready | 需要 staging validation |

### P4-owned Gates

| Gate | 状态 |
|------|------|
| identity-access-module:test | ✅ 361/361 |
| platform-app:test | ✅ BUILD SUCCESSFUL |
| Frontend typecheck | ✅ 0 errors |
| ImportedMetadataPanel | ✅ 9/9 |

### Manual Review Pending

- Security sign-off
- Golden Render visual QA
- Infrastructure inputs
- Modulith debt decision
- Frontend UI/UX review
- CI/test strategy review

### Staging Readiness Gate

详见：[Staging Readiness Gate](staging-readiness-gate-2026-06-08.md)

### Human Review Execution

详见：[Human Review Execution](human-review-execution-2026-06-08.md)

### Human Review Tracker

详见：[Human Review Tracker](human-review-tracker-2026-06-08.md)

**当前状态:** RC Ready / Human Review Pending / Staging Pending / Production Not Ready
