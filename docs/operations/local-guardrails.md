# Local Guardrails — media-platform

**Date:** 2026-07-01
**Status:** ACTIVE
**Authority:** HOOKS.0

---

## Purpose

Reduce accidental unsafe commits during autonomous development by adding lightweight local checks.

## What Is Mandatory

| Check | Tool | Enforcement |
|-------|------|-------------|
| AGENT_TASK.md not committed | pre-commit hook | BLOCK |
| ARCHITECTURE_MAP_LINKS.md not committed | pre-commit hook | BLOCK |
| .env / .env.* not committed | pre-commit hook | BLOCK |
| Flyway V1 baseline not modified | pre-commit hook | BLOCK |
| Private keys not committed | pre-commit hook | BLOCK |
| Trailing whitespace | pre-commit hook | AUTO-FIX |
| End of file fixer | pre-commit hook | AUTO-FIX |
| YAML/JSON syntax | pre-commit hook | BLOCK |
| Merge conflict markers | pre-commit hook | BLOCK |
| Large files (>500KB) | pre-commit hook | BLOCK |

## What Is Warning-Only

| Check | Reason |
|-------|--------|
| Forbidden architecture wording | Context-dependent; cannot reliably distinguish "must not" from active claims. CI/Semgrep performs stronger checks. Skips .md/.txt and semgrep config files. |

## What Is Intentionally Excluded

| Check | Reason |
|-------|--------|
| Full test suite | Too slow for pre-commit; agents run tests separately |
| Compile check | Too slow for pre-commit |
| LikeC4 validation | Not every commit touches LikeC4 |
| Semgrep full scan | CI performs full scan |

## Agent Instructions

1. Run `pre-commit run --all-files` before local commit when available
2. Report hook failures in Review Packet
3. Do not bypass hooks unless task explicitly allows
4. Hooks are not a substitute for tests or review

## Bypass

Only with explicit human authorization:
```bash
git commit --no-verify -m "commit message"
# Must document reason in Review Packet
```

---

*Generated by Hermes Agent — HOOKS.0*
