# Frontend Routing & State Plan — media-platform

**Date:** 2026-07-01
**Status:** PLANNED
**Authority:** FRONTEND-MODERNIZE.0

---

## Routes

| Route | Component | Data |
|-------|-----------|------|
| `/` | Dashboard | Project summary |
| `/projects` | ProjectList | All projects |
| `/projects/:id` | ProjectDetail | Project + timeline |
| `/projects/:id/editor` | Editor | Full editor |
| `/projects/:id/render-jobs` | RenderJobs | Job history |
| `/products/:id` | ProductDetail | Product + artifacts |
| `/settings/providers` | ProviderSettings | Provider config |
| `/settings/workers` | WorkerSettings | Worker config |

## State Architecture

```
Global State (Zustand)
├── auth: AuthState
├── ui: UIState (sidebar, theme)
└── notifications: NotificationState

Server State (TanStack Query)
├── projects: Query<Project[]>
├── timeline: Query<Timeline>
├── renderJobs: Query<RenderJob[]>
├── products: Query<Product[]>
└── providers: Query<Provider[]>

Editor State (Zustand)
├── timeline: TimelineStore
├── captions: CaptionStore
├── selection: SelectionStore
└── history: HistoryStore
```

## API Integration

```
Component → useQuery/useMutation → API client (fetch) → Backend
```

### Contracts

- All API responses validated with Zod
- Contracts in `api/contracts/`
- Type inference from Zod schemas

## Error Handling

| Level | Tool |
|-------|------|
| API errors | TanStack Query retry + error boundary |
| Component errors | React Error Boundary |
| Global errors | Sentry |
| Auth errors | Auth interceptor → login redirect |

## Loading States

| Pattern | Use |
|---------|-----|
| Skeleton | List pages |
| Spinner | Detail pages |
| Optimistic | Mutations |
| Suspense | Route transitions |

---

*Generated by Hermes Agent — FRONTEND-MODERNIZE.0*
