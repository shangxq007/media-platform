package com.example.platform.compatibility.domain;

/**
 * Identifies a schema family for versioned objects.
 */
public enum SchemaFamily {
    OTIO_TIMELINE,
    EFFECT_PACK,
    EFFECT_METADATA,
    RENDER_PRESET,
    EXPORT_POLICY,
    ARTIFACT_METADATA,
    USER_ANALYTICS_EVENT,
    USER_PROFILE,
    PROMPT_TEMPLATE,
    PROVIDER_CAPABILITY
}
