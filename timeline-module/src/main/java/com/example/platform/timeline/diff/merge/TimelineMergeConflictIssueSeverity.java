package com.example.platform.timeline.diff.merge;

/**
 * Severity of a merge conflict issue. Internal domain model.
 */
public enum TimelineMergeConflictIssueSeverity {
    INFO,
    WARNING,
    ERROR,
    BLOCKING
}
