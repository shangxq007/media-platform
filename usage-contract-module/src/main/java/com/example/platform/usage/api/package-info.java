/**
 * Usage-owned immutable observation and ingestion contracts. Producers own execution facts;
 * Billing owns accounting interpretation, never execution or admission decisions.
 */
@org.springframework.modulith.NamedInterface("observations")
package com.example.platform.usage.api;
