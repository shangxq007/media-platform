package com.example.platform.shared.commercial;

/** Canonical commercial principal kinds; authentication-provider identities are not included. */
public enum PrincipalType {
    USER,
    SERVICE_ACCOUNT,
    WORKSPACE,
    ORGANIZATION
}
