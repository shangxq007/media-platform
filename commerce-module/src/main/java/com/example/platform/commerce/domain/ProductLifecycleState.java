package com.example.platform.commerce.domain;

/** Lifecycle of stable product identity, independent from sellable offering versions. */
public enum ProductLifecycleState {
    DRAFT,
    ACTIVE,
    RETIRED
}
