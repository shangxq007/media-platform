package com.example.platform.providerplugin.visual;

/** Provider-runtime classification of a render capability support declaration. */
public enum ProviderVisualCapabilityCategory {
    CAPTION, WATERMARK, OVERLAY, EFFECT, TRANSITION, COLOR, TRANSFORM, AUDIO, PACKAGING
}
