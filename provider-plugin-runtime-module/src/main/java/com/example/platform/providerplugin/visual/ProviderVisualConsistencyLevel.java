package com.example.platform.providerplugin.visual;

/** Provider-declared fidelity of one visual capability implementation. */
public enum ProviderVisualConsistencyLevel {
    EXACT, APPROX, PROVIDER_SPECIFIC, UNSUPPORTED, FORBIDDEN, UNKNOWN
}
