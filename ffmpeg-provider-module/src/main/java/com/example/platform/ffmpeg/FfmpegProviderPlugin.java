package com.example.platform.ffmpeg;

import org.pf4j.Plugin;
import org.pf4j.PluginWrapper;

/** PF4J lifecycle shell for the independently distributable FFmpeg provider. */
public final class FfmpegProviderPlugin extends Plugin {

    public FfmpegProviderPlugin(PluginWrapper wrapper) {
        super(wrapper);
    }
}
