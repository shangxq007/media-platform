/**
 * Provider-local executable-task composition contracts exposed to execution-fabric consumers.
 *
 * <p>ROADMAP_22_EPOCH_3: these immutable task memberships are evaluated in the frozen composition
 * layer; the execution fabric consumes them downstream.
 */
@org.springframework.modulith.NamedInterface("composition")
package com.example.platform.execution.composition;
